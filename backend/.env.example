ALLOWED_ORIGINS=https://localhost:3000,https://localhost:5173
API_PREFIX=/api     # all API requests or routes will begin with this prefix
DEBUG=True          # may be needed for deployment

# Runs locally for now; will eventually use PostgreSQL
DATABASE_URL=sqlite:///./database.db

# OPEN_AI_KEY=
